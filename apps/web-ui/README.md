# Web UI Placeholder

This directory is reserved for the upcoming local web interface.

Planned purpose:

- Provide a browser-based UI for interacting with local language-study data.
- Communicate with a local C++ service/API process.
- Keep web concerns (frontend app, client build tooling, API contracts) isolated from the C++ core and tooling scripts.

Suggested future structure:

- `src/` for UI code.
- `public/` for static assets.
- `package.json` for frontend dependency management.
- `README.md` updates with run/build instructions once scaffolded.
