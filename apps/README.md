# Apps

Application components live under this directory.

- `cpp-cli/`: existing C++ study engine and CLI source.
- `web-ui/`: future browser UI that will interact with a local C++ service.

Keeping apps grouped here makes it easier to expand from a single executable into multiple runtime components.
